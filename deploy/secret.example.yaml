# 示例：LLM api_key 通过 Secret 注入（绝不写进 ConfigMap）。
# 使用方法：复制为 secret.yaml 并替换 REPLACE_ME 后 apply。
apiVersion: v1
kind: Secret
metadata:
  name: k8s-ai-llm
  namespace: k8s-ai
  labels:
    app: k8s-ai
type: Opaque
stringData:
  api_key: "REPLACE_ME"