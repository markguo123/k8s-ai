apiVersion: v1
kind: Service
metadata:
  name: k8s-ai-server
  namespace: k8s-ai
  labels:
    app: k8s-ai
    component: server
spec:
  selector:
    app: k8s-ai
    component: server
  ports:
    - name: http
      port: 8080
      targetPort: http